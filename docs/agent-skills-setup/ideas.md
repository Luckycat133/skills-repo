# Ideas

- add `--skill <name>` filtering to the sync script
- 给同步脚本增加 `--skill <name>` 精确过滤
- add a dedicated `doctor` script for skill integrity checks
- 增加专用 `doctor` 脚本做 skill 完整性检查
- add a reusable license and release template for public repositories
- 增加可复用的公开仓库 License 和发布模板
- add a changelog workflow for skill releases
- 增加 skill 发布的 changelog 工作流
- add a ClawHub publish helper that prepares slug, version, tags, and changelog text
- 增加 ClawHub 发布辅助脚本，自动准备 slug、版本、标签和 changelog
- add a distribution pack generator for README badges, screenshots, and install snippets
- 增加分发素材生成器，自动生成 README 徽章、截图占位和安装片段