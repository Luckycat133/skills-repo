# validation_data — 验证集

> **用途**：超参数调优、早停（early stopping）、模型选择与开发期评估。
> **性质**：不参与参数拟合，但会被多次评估，因此**不能**代表最终无偏性能。

---

## 1. 数据来源（Data Source）
- **来源说明**：_（通常从训练数据中留出，与训练同分布）_
- **占比**：约占**总训练数据的 15%–20%**（详见根目录 `DATA_USAGE_GUIDE.md`）。

## 2. 数据格式（Format）
- **文件格式**：_（如 CSV / Parquet / JSONL）_
- **编码**：UTF-8
- **文件位置**：
  - `raw/`：从原始数据留出的验证子集
  - `processed/`：经与训练一致的预处理后的数据
  - `metadata/`：schema、特征字典、标签映射

## 3. 特征含义（Feature Dictionary）
| 字段名 | 类型 | 含义 | 取值范围/单位 | 备注 |
|--------|------|------|----------------|------|
| id | string | 样本唯一标识 | — | 不参与建模 |
| feature_1 | float | _（示例）_ | _（示例）_ | |
| ... | ... | ... | ... | |
| label | int/str | 目标标签 | _（示例）_ | 评估目标 |

> 完整版建议维护在 `metadata/feature_dictionary.md`。

## 4. 划分方法（Split Method）
- **方法**：分层随机抽样，与训练/测试保持同一标签分布。
- **随机种子**：`42`
- **生成命令**：见根目录 `scripts/split_dataset.py`
- **一致性校验**：`scripts/verify_distribution.py`

## 5. 使用约束
- 可多次用于调参与早停；调参完成后建议将验证集并回训练集重训最终模型（可选策略）。
- 与测试集**不得重叠**。
