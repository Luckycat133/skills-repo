name: Validate skills

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  validate:
    name: Validate (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    steps:
      - uses: actions/checkout@v7
      - name: Install pinned official Agent Skills validator
        # --break-system-packages: macOS runners resolve python3 to the
        # PEP 668-managed interpreter; setup-python environments ignore
        # the flag, so this is safe across the matrix.
        run: >-
          python3 -m pip install --break-system-packages
          'git+https://github.com/agentskills/agentskills.git@69ef37e9424c0a7ea9dd2293b559e43ec8176379#subdirectory=skills-ref'
      - name: Validate repository & run full test suites
        if: matrix.os != 'windows-latest'
        run: bash validate-all.sh
      - name: Validate repository & run full test suites (Windows)
        if: matrix.os == 'windows-latest'
        shell: bash
        run: bash validate-all.sh
